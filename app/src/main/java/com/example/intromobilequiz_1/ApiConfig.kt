package com.example.intromobilequiz_1

/**
 * Central place to configure API endpoints.
 * Using a single source of truth prevents hardcoded URLs from spreading around the codebase.
 */
object ApiConfig {
    const val USERS_URL: String = "https://jsonplaceholder.typicode.com/users"
}